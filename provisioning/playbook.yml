- name: Hello world
  hosts: debian-desktop
  gather_facts: yes
  become: yes
  roles:
    - name: user-access
      user_access_username: user
      user_access_password: Password123
      user_access_sudo: True
  tasks:
    - name: Set password for mgmt_user
      ansible.builtin.user:
        name: debian
        password: "{{ 'qwert1234' | password_hash('sha512') }}"    
    - name: Print hello world
      debug:
        msg: "Hello World"
